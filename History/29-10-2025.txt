29/10/2025

# Whats added/removed

- Organizing project files
- Improving Math library with:
    + Matrix4
    + Matrix Utils
    + Primitive Shapes (Rect2D, Circle2D)
    + Geometry2D Utils
- Removed Shape2D base class with it drived classes
- Improving the Collision2D object with the new shape logic (direct verts)
- Making the first renderer with batching support + 3 draw methods (verts, lines, filled)

# Summary
- Going deep into math library and making the first renderer for the engine for algorithm/sys
visualization

# Details
- For now the renderer support only "Convex Polygons" cause of the simple triangulation algorithm O(n)